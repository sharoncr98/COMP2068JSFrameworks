<h1>{{title}}</h1>
<a href="/workouts/add" class="btn btn-primary mb-3">Add New Workout</a>
<div class="row">
  {{#if workouts.length}}
    {{#each workouts}}
      <div class="col-md-6 mb-3">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">{{this.type}}</h5>
            <p class="card-text">
              <strong>Duration:</strong> {{this.duration}} minutes<br>
              {{#if this.calories}}
                <strong>Calories:</strong> {{this.calories}}<br>
              {{/if}}
              <strong>Date:</strong> {{this.date}}<br>
              {{#if this.notes}}
                <strong>Notes:</strong> {{this.notes}}
              {{/if}}
            </p>
            <a href="/workouts/edit/{{this._id}}" class="btn btn-sm btn-warning">Edit</a>
            <form method="POST" action="/workouts/delete/{{this._id}}" style="display:inline;">
              <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure?')">Delete</button>
            </form>
          </div>
        </div>
      </div>
    {{/each}}
  {{else}}
    <div class="col-12">
      <div class="alert alert-info">
        No workouts
      </div>
    </div>
  {{/if}}
</div>